import { Meta, Canvas, Controls, Unstyled } from '@storybook/addon-docs/blocks';
import * as BadgeStories from './Badge.stories';
import { TokenTable } from '../../lib/TokenTable';
import { Badge } from './Badge';

<Meta of={BadgeStories} />

# Badge

A compact inline label for status, category, or count — six semantic variants with optional leading icon.

## Usage

```tsx
import { Badge } from 'packt-ds';

<Badge variant="brand">New</Badge>
<Badge variant="success">Verified</Badge>
```

---

## Playground

<Canvas of={BadgeStories.Playground} />
<Controls of={BadgeStories.Playground} />

---

## Variants

<Canvas of={BadgeStories.Variants} />

<Unstyled>
  <div style={{ fontFamily: 'Outfit, sans-serif' }}>
    {[
      { variant: 'neutral', bg: '#e4e4e4', color: '#4d4d4d', desc: 'No semantic meaning' },
      { variant: 'brand',   bg: '#ffe3d8', color: '#cd603a', desc: 'Brand highlight — Packt orange' },
      { variant: 'hub',     bg: '#dbe9fe', color: '#2463eb', desc: 'Hub-specific highlight' },
      { variant: 'info',    bg: '#dbe9fe', color: '#1c4ed8', desc: 'Informational state' },
      { variant: 'success', bg: '#d9fee3', color: '#279544', desc: 'Positive / completed state' },
      { variant: 'warning', bg: '#fcf6cc', color: '#9b8a0d', desc: 'Caution or action needed' },
      { variant: 'error',   bg: '#f9d2d9', color: '#921d33', desc: 'Error or critical state' },
    ].map(({ variant, bg, color, desc }, i, arr) => (
      <div key={variant} style={{ display: 'flex', alignItems: 'center', gap: 16, padding: '14px 16px', borderBottom: i < arr.length - 1 ? '1px solid #e4e4e4' : 'none', background: i % 2 === 0 ? '#ffffff' : '#fafafa' }}>
        <code style={{ width: 64, fontSize: 12, color: '#4d4d4d', flexShrink: 0 }}>{variant}</code>
        <div style={{ width: 80, flexShrink: 0, display: 'flex', justifyContent: 'center' }}>
          <Badge variant={variant}>{variant.charAt(0).toUpperCase() + variant.slice(1)}</Badge>
        </div>
        <span style={{ fontSize: 13, color: '#797979' }}>{desc}</span>
      </div>
    ))}
  </div>
</Unstyled>

---

## With icon

Pass any React node to the `icon` prop; it is sized and aligned automatically.

<Canvas of={BadgeStories.WithIcon} />

---

## Tokens used

Every colour is a Tailwind utility wired to tokens in `src/styles/tailwind.css`, which reads the theme-aware aliases in `src/tokens/tokens.theme.css`. Each alias resolves to a different raw variable per `data-theme` — the swatches below show both, resolved live.

<TokenTable rows={[
  { property: 'background page', utility: 'bg-bg-page', cssVar: '--packt-background-page' },
  { property: 'brand tag bg', utility: 'bg-brand-tag-bg', cssVar: '--packt-brand-tag-bg' },
  { property: 'hub packt tag bg', utility: 'bg-hub-tag-bg', cssVar: '--packt-hub-packt-tag-bg' },
  { property: 'status bg error', utility: 'bg-status-bg-error', cssVar: '--packt-status-bg-error' },
  { property: 'status bg info', utility: 'bg-status-bg-info', cssVar: '--packt-status-bg-info' },
  { property: 'status bg success', utility: 'bg-status-bg-success', cssVar: '--packt-status-bg-success' },
  { property: 'status bg warning', utility: 'bg-status-bg-warning', cssVar: '--packt-status-bg-warning' },
  { property: 'border default', utility: 'border-border-default', cssVar: '--packt-border-default' },
  { property: 'brand border default', utility: 'border-brand-border-default', cssVar: '--packt-brand-border-default' },
  { property: 'hub packt border default', utility: 'border-hub-border-default', cssVar: '--packt-hub-packt-border-default' },
  { property: 'status border error', utility: 'border-status-border-error', cssVar: '--packt-status-border-error' },
  { property: 'status border info', utility: 'border-status-border-info', cssVar: '--packt-status-border-info' },
  { property: 'status border success', utility: 'border-status-border-success', cssVar: '--packt-status-border-success' },
  { property: 'status border warning', utility: 'border-status-border-warning', cssVar: '--packt-status-border-warning' },
  { property: 'brand tag text', utility: 'text-brand-tag-text', cssVar: '--packt-brand-tag-text' },
  { property: 'content secondary', utility: 'text-content-secondary', cssVar: '--packt-content-secondary' },
  { property: 'hub packt tag text', utility: 'text-hub-tag-text', cssVar: '--packt-hub-packt-tag-text' },
  { property: 'status error text', utility: 'text-status-text-error', cssVar: '--packt-status-error-text' },
  { property: 'status text info', utility: 'text-status-text-info', cssVar: '--packt-status-text-info' },
  { property: 'status text success', utility: 'text-status-text-success', cssVar: '--packt-status-text-success' },
  { property: 'status text warning', utility: 'text-status-text-warning', cssVar: '--packt-status-text-warning' },
]} />
