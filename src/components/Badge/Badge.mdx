import { Meta, Canvas, Controls, Unstyled } from '@storybook/addon-docs/blocks';
import * as BadgeStories from './Badge.stories';
import { Badge } from './Badge';

<Meta of={BadgeStories} />

# Badge

A compact inline label for status, category, or count — six semantic variants with optional leading icon.

## Usage

```tsx
import { Badge } from 'packt-ds';

<Badge variant="brand">New</Badge>
<Badge variant="success">Verified</Badge>
```

---

## Playground

<Canvas of={BadgeStories.Default} />
<Controls of={BadgeStories.Default} />

---

## Variants

<Canvas of={BadgeStories.Variants} />

<Unstyled>
  <div style={{ fontFamily: 'Outfit, sans-serif' }}>
    {[
      { variant: 'default', bg: '#e4e4e4', color: '#4d4d4d', desc: 'Neutral — no semantic meaning' },
      { variant: 'brand',   bg: '#f97141', color: '#ffffff', desc: 'Brand highlight — Packt orange' },
      { variant: 'info',    bg: '#2563eb', color: '#ffffff', desc: 'Informational state' },
      { variant: 'success', bg: '#15803d', color: '#ffffff', desc: 'Positive / completed state' },
      { variant: 'warning', bg: '#ca8a04', color: '#ffffff', desc: 'Caution or action needed' },
      { variant: 'error',   bg: '#df1c41', color: '#ffffff', desc: 'Error or critical state' },
    ].map(({ variant, bg, color, desc }, i, arr) => (
      <div key={variant} style={{ display: 'flex', alignItems: 'center', gap: 16, padding: '14px 16px', borderBottom: i < arr.length - 1 ? '1px solid #e4e4e4' : 'none', background: i % 2 === 0 ? '#ffffff' : '#fafafa' }}>
        <code style={{ width: 64, fontSize: 12, color: '#4d4d4d', flexShrink: 0 }}>{variant}</code>
        <div style={{ width: 80, flexShrink: 0, display: 'flex', justifyContent: 'center' }}>
          <Badge variant={variant}>{variant.charAt(0).toUpperCase() + variant.slice(1)}</Badge>
        </div>
        <span style={{ fontSize: 13, color: '#797979' }}>{desc}</span>
      </div>
    ))}
  </div>
</Unstyled>

---

## With icon

Pass any React node to the `icon` prop; it is sized and aligned automatically.

<Canvas of={BadgeStories.WithIcon} />

---

## Tokens used

<Unstyled>
  <div style={{ fontFamily: 'Outfit, sans-serif', fontSize: 13, border: '1px solid #e4e4e4', borderRadius: 8, overflow: 'hidden' }}>
    <div style={{ display: 'flex', gap: 8, padding: '10px 16px', background: '#1a1a1a', color: '#ffffff', fontSize: 11, fontWeight: 600 }}>
      <span style={{ flex: '0 0 200px' }}>Property</span>
      <span style={{ flex: 1 }}>Token</span>
    </div>
    {[
      { property: 'Background · brand',   hex: '#f97141', token: '--packt-semantic-colors-light-background-brand-selected' },
      { property: 'Background · info',    hex: '#2563eb', token: '--packt-semantic-colors-light-background-info-selected' },
      { property: 'Background · success', hex: '#15803d', token: '--packt-green-700' },
      { property: 'Background · warning', hex: '#ca8a04', token: '--packt-yellow-600' },
      { property: 'Background · error',   hex: '#df1c41', token: '--packt-semantic-colors-light-background-error-selected' },
      { property: 'Background · default', hex: '#e4e4e4', token: '--packt-neutral-200' },
      { property: 'Border',               hex: '#e4e4e4', token: '--packt-width-xs (1px solid transparent per variant)' },
      { property: 'Radius',               hex: null,      token: '--packt-radius-pill' },
      { property: 'Font size',            hex: null,      token: '--packt-size-10' },
    ].map(({ property, hex, token }, i, arr) => (
      <div key={token} style={{ display: 'flex', alignItems: 'center', gap: 8, padding: '10px 16px', borderBottom: i < arr.length - 1 ? '1px solid #e4e4e4' : 'none', background: i % 2 === 0 ? '#ffffff' : '#fafafa' }}>
        <span style={{ flex: '0 0 200px', fontSize: 12, color: '#4d4d4d', fontWeight: 500 }}>{property}</span>
        <div style={{ flex: 1, display: 'flex', alignItems: 'center', gap: 8 }}>
          {hex && <div style={{ width: 18, height: 18, borderRadius: 4, background: hex, border: '1px solid #e4e4e4', flexShrink: 0 }} />}
          <code style={{ fontSize: 11, color: '#797979' }}>{token}</code>
        </div>
      </div>
    ))}
  </div>
</Unstyled>
