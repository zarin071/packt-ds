import { Meta, Canvas, Controls } from '@storybook/addon-docs/blocks';
import * as SiteFooterStories from './SiteFooter.stories';

<Meta of={SiteFooterStories} />

# SiteFooter

Link columns — each a labelled `<nav>` landmark — plus [SocialLinks](?path=/docs/molecules-sociallinks--docs) and a copyright line, all inside the page's `contentinfo` landmark.

## Playground

<Canvas of={SiteFooterStories.Playground} />
<Controls of={SiteFooterStories.Playground} />
